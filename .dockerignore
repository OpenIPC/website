# The only image built from the repository root is the Go service's
# (service/Dockerfile). It needs service/ and the catalogue, and nothing else:
# an allowlist, so a new top-level directory cannot quietly grow the build
# context or carry something private into it.
*
!service
!data/catalogue

# The Go service's build output and goldens.
service/bin
service/**/testdata
