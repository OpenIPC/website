<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to t('devise.confirmations.confirm'), confirmation_url(@resource, confirmation_token: @token) %></p>
