# meta-externalagent (Meta AI-training crawler) crawls the Open Wall in
# violation of robots.txt, at >50% of all traffic during the 2026-08/09 scrape
# wave. facebookexternalhit (link previews) is a different agent and stays
# allowed.
map $http_user_agent $openipc_blocked_crawler {
    default 0;
    ~*meta-externalagent 1;
}
