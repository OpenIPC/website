---
zh:
  activerecord:
    attributes:
      snapshot:
        mac_address: MAC地址
      soc:
        build_status_url: 构建状态 URL
        kernel: 内核
        linux_filename: 文件
        load_address: 加载地址
        model: 芯片型号
        notes: 备注
        sdk: 开发工具包
        status: 阶段
        uboot_filename: U-Boot文件
        vendor_id: SoC供应商
        version: 版本
    help:
      camera:
        flash_type: 如果不确定，请选择 NOR 8M。
        partition_layout: 默认与芯片容量一致。在更大的芯片上使用 8MB 分区布局会把余下的空间留给 overlay，但只能容纳 Lite 版本。
    models:
      snapshot: 快照
      soc: 芯片型号
