import type { Meta, StoryObj } from '@storybook/preact-vite';
import Footer from './footer';

const meta: Meta = {
  component: Footer,
  title: 'Design System/Widgets/Footer',
}

export default meta;
type Story = StoryObj<typeof meta>;

export const FooterStory: Story = {};
